class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        arr=[]
        for i in range(0,len(nums)):
            nums.sort()
            if nums[i]==target:
                arr.append(i)
        return arr
        
