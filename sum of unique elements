class Solution:
    def sumOfUnique(self, nums: List[int]) -> int:
        dicto={}
        for i in range(0,len(nums)):
            if nums[i] in dicto:
                dicto[nums[i]]+=1
            else:
                dicto[nums[i]]=1
        sum=0
        for i in dicto:
            if dicto[i]==1:
                sum+=i
        return sum
            
