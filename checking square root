class Solution:
    def isPerfectSquare(self, num: int) -> bool:
        square=False
        for i in range(num+1):
            if i*i==num:
                square=True
                break
        if square:
            return True
        else:
            return False
        
